Gaming Leaderboard System - Backend

This backend is a static JSON file used to simulate
player score storage.

Endpoint (demo):
GET /backend/leaderboard-data.json

Used only for assignment demonstration.
